name: Build and Release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      # Linux ARM64 dependencies
      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libarchive-tools fakeroot dpkg-dev

      # Windows specific Cert setup (preserved from original)
      - name: Decode Windows signing certificate (PFX)
        if: matrix.os == 'windows-latest'
        id: decode_cert
        env:
          WIN_CERT_BASE64: ${{ secrets.WIN_CERT_BASE64 }}
        run: |
          if ([string]::IsNullOrEmpty($env:WIN_CERT_BASE64)) { return }
          $certBase64 = $env:WIN_CERT_BASE64
          $certBytes = [System.Convert]::FromBase64String($certBase64)
          $certPath = [System.IO.Path]::Combine([System.IO.Path]::GetTempPath(), "win_cert.p12")
          [System.IO.File]::WriteAllBytes($certPath, $certBytes)
          Write-Host "Wrote cert to $certPath"
          echo "CSC_LINK=file://$certPath" >> $env:GITHUB_ENV
          echo "CSC_KEY_PASSWORD=${{ secrets.WIN_CERT_PASSWORD }}" >> $env:GITHUB_ENV
        shell: powershell

      - name: Install NSIS (for Windows installer)
        if: matrix.os == 'windows-latest'
        run: choco install nsis -y

      - name: Build Windows
        if: matrix.os == 'windows-latest'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npm run build

      - name: Build Linux ARM64
        if: matrix.os == 'ubuntu-latest'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USE_HARD_LINKS: false
        run: npm run build:linux-arm

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts-${{ matrix.os }}
          path: |
            release-builds/*.exe
            release-builds/*.exe.blockmap
            release-builds/*.AppImage
            release-builds/*.deb
          if-no-files-found: ignore

  publish:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./dist
          merge-multiple: true

      - name: Create GitHub Release and upload artifacts
        uses: ncipollo/release-action@v1
        with:
          name: "Emerald Timer ${{ github.ref_name }}"
          body: |
            ## 💎 Emerald Timer ${{ github.ref_name }} 正式版 
            此版本包含 UI 性能优化、大比例缩放支持、统计网格并列布局。

            ### 🌟 新特性 (New Features)
            * **动态缩放控制**：支持 10%-300% 范围内的精准 UI 缩放。
            * **并列布局优化**：月统计图表在大屏下自动分栏，消除空白空间。
            * **矩形周视图**：周视图采用更舒展的长方形卡片设计，提升信息密度。
            * **UI 响应性**：调大统计网格日期和时长文字，视距更清晰。
            
            ### 🔧 平台稳定性 (Fixes & Improvements)
            * **Babel 语法修护**：修正了 MiniCalendar 组件中的冗余 JSX 闭合标签。
            * **流程同步**：统一主进程入口为 `main.cjs` 确保构建一致性。
            * **CI 工作流修复**：清理了 `release.yml` 中的冲突字段，恢复版本发布功能。

            ---
            *本版本由 GitHub Actions 自动构建发布。*
          artifacts: "dist/*"
          token: ${{ secrets.GITHUB_TOKEN }}
          tag: ${{ github.ref_name }}
          allowUpdates: true
          draft: false
          prerelease: false
